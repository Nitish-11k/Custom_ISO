# GRUB Configuration for Custom ISO
# Background image + graphical Python app

set timeout=20
set default=0

# ── Graphics setup ──────────────────────────────
insmod vbe
insmod vga
insmod video_bochs
insmod video_cirrus
insmod gfxterm
insmod png

set gfxmode=1024x768x32,800x600x32,auto

if terminal_output gfxterm ; then
    background_image /boot/grub/background.png
fi

# ── Menu colors ─────────────────────────────────
set menu_color_normal=dark-gray/black
set menu_color_highlight=green/white
set color_normal=dark-gray/black
set color_highlight=green/white

# ── Menu Entries ────────────────────────────────

menuentry ">> Launch Custom Dashboard <<" {
    echo "Loading Tiny Core Linux..."
    echo "Loading Extensions from CD..."
    # cde: load extensions from CD
    # waitusb=20: wait longer for CD/USB detection
    # showapps: shows extension loading progress
    # noswap: skip swap search
    # tce=cde: explicitly tell tce to look at cde
    linux /boot/vmlinuz64 loglevel=3 cde tce=cde waitusb=20 vga=791 showapps noswap
    initrd /boot/core_custom.gz /boot/modules64.gz
}

menuentry "Reboot" {
    reboot
}

menuentry "Power Off" {
    halt
}
